import { ChangeDetectionStrategy, Component, EventEmitter, Input, OnInit, Output } from '@angular/core';import { WeatherInterface } from '../types/weather.interface';@Component({  selector: 'app-widget-page',  templateUrl: './widget-page.component.html',  styleUrls: ['./widget-page.component.css'],  changeDetection: ChangeDetectionStrategy.OnPush})export class WidgetPageComponent implements OnInit{  @Input() weather!: WeatherInterface;  @Output() deleteEvent: EventEmitter<string> = new EventEmitter<string>();  public latitude!: number;  public longitude!: number;  ngOnInit(): void {    this.getCoords()  }  public onDelate(): void {    this.deleteEvent.emit(this.weather.name)  }  private getCoords(): void {    this.latitude = Math.round(this.weather.coord.lat)    this.longitude = Math.round(this.weather.coord.lon)  }}